{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["^class/defaults.yml$"],
      "matchStrings": [
        "suc_image:[[:space:]](?<depName>.*?):(?<currentValue>.*?)$"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
